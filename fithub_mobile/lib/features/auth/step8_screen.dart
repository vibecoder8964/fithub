import 'package:flutter/material.dart';

class Step8Screen extends StatelessWidget {
  const Step8Screen({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder: allow/deny notifications, camera, recorder and trigger AI workout generation.
    return const Scaffold(
      body: Center(
        child: Text('Step 8 - Enable notifications and access'),
      ),
    );
  }
}

